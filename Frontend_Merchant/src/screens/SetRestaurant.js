import { StyleSheet, Text, View, SafeAreaView } from "react-native";
import React from "react";

const SetRestaurant = () => {
    return (<SafeAreaView>
        <Text> Set Restaurant </Text>
    </SafeAreaView>)
};

export default SetRestaurant;

const styles = StyleSheet.create({});
