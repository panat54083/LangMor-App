const router = require("express").Router();
const { catchErrrors } = require("../handler/errorHandler");
const restaurantController = require("../controllers/restaurantController");

module.exports = router;
