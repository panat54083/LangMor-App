import { StyleSheet, Text, View } from "react-native";
import React from "react";

const FoodListHeader = (props) => {
    const { imgSrc } = pros;
    return (
        <View>
            <Text>FoodListHeader</Text>
        </View>
    );
};

export default FoodListHeader;

const styles = StyleSheet.create({});
